rule "File Named Route Export"
fact file_named_export

find export [GET,POST]

let method =
  from export take name

let filePath =
  from file take path

let handler =
  from export take reference

build {
  method: method
  path: filePath | normalize fileRoutePath
  handler: handler
}
