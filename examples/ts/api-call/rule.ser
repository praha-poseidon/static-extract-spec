rule "Spec TS API Call"
fact frontend_api_call

find call axios

let method =
  from call take method

let path =
  from argument[0] take value

build {
  client: "axios"
  method: method | normalize upper
  path: path
}
